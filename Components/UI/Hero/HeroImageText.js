import React from 'react'

function HeroImageText({ image, title, subtitle }) {
    console.log(image)
    return (
        <Container>
            hero
        </Container>
    )
}

export default HeroImageText
